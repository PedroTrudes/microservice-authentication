{
  "name": "ms-authentication",
  "version": "1.0.0",
  "description": "Microservice crriado durante a live da DIO",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./",
    "build": "tsc -p .",
    "dev": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/index.ts"
  },
  "author": "Pedro Trudes",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.10.4",
    "express": "^4.18.2",
    "http-status-codes": "^2.3.0",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0"
  }
}
